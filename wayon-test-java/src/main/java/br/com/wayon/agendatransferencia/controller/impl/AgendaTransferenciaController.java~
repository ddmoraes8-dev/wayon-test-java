package br.com.wayon.agendatransferencia.controller.impl;

import io.swagger.annotations.Api;
import io.swagger.annotations.ApiOperation;
import io.swagger.annotations.ApiParam;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

@RestController
@RequestMapping("/${app.version}/agenda-transferencia")
@Api(tags = "Agenda Transferencia")
public class AgendaTransferenciaController{
    @ApiOperation(value = "Buscar usuário por ID", notes = "Forneça um ID válido para buscar um usuário.")
    @GetMapping("/extrato/{id}")
    public String consultaExtratoPorId(
            @ApiParam(value = "ID do usuário", required = true)
            @PathVariable String id) {
        return null;
    }
}
